snapshots:
  - name: silver_properties_scd2
    relation: ref('silver_properties')
    config:
      target_schema: "{{ var('silver.target_schema') }}"
      alias: silver_properties
      unique_key: property_id_sk
      strategy: check
      check_cols: [title, description, base_price, property_type, max_guests, bathrooms, bedrooms]
      hard_deletes: invalidate
      snapshot_meta_column_names:
        dbt_valid_from: record_start_date
        dbt_valid_to: record_end_date
        dbt_scd_id: record_scd_id
        dbt_updated_at: record_updated_at
        dbt_is_deleted: record_is_deleted

