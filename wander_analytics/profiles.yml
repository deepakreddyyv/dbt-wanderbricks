wander_analytics:
  target: dev
  outputs:
    dev:
      type: databricks
      catalog: wander_analytics #optional catalog name if you are using Unity Catalog]
      schema: bronze # Required
      host: dbc-25d9d64d-9c9e.cloud.databricks.com # Required
      http_path: /sql/1.0/warehouses/5da2b8f6fe4099dc # Required
      token: "{{ env_var('DBT_DATABRICKS_TOKEN') }}" # Required Personal Access Token (PAT) if using token-based authentication
      threads: 4  # Optional, default 1